# Plan: {{feature title}}

Status: draft
Job: {{dev_jobs.id}}
Spec: {{link to the approved spec.md this plan implements}}

## 1. Definition of done

The short list of conditions that must all be true for this to be considered
complete — usually a restatement of the spec's acceptance criteria in
implementation terms.

## 2. File map

| File | Change |
|---|---|
| path/to/file.ext | what changes here |

## 3. User journey

A short walkthrough of how a user (or another part of the system) exercises
this feature once it's built, so the reviewer can sanity-check the plan against
real usage before any code exists.

## 4. Tasks

Numbered, in the order they should be done. Each task should be small enough
to write a failing test for, implement, and verify independently.

- [ ] 1. {{task}} — files: {{files touched}} — test: {{what proves it works}}
- [ ] 2. {{task}} — files: {{files touched}} — test: {{what proves it works}}
- [ ] 3. {{task}} — files: {{files touched}} — test: {{what proves it works}}

## 5. Test plan

How the whole feature gets verified beyond the per-task tests above — any
integration or end-to-end check that only makes sense once every task is done.

## 6. Out of scope (carried from spec)

Restated here as a reminder during implementation — anything not to touch.
